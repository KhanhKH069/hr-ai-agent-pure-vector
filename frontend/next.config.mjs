/** @type {import('next').NextConfig} */
const nextConfig = {
  // Allow dev server to be accessed from network interfaces
  allowedDevOrigins: ['localhost', '127.0.0.1']
};

export default nextConfig;

